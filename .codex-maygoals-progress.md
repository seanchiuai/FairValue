# FairValue May Goals Progress

Last updated: 2026-05-27 04:34:52 PDT

## Source Contract

- Source of truth: `maygoals.md`
- Objective: fully pursue FairValue Atlas, the universal property intelligence and prediction protocol where every real-estate asset can be modeled, debated, traded, simulated, audited, replayed, and settled as a living market.
- Practical interpretation: the full planetary scope is intentionally impossible, so progress is measured by verified, repo-native slices that make the requested final state more true without fake capability.

## Repo Inventory

- Package manager: npm (`package-lock.json`, npm 11.12.1, Node v25.9.0 observed).
- App stack: React 19, TypeScript, Vite frontend; Node.js/Express 5 backend; `ws` WebSocket realtime; Neon/Postgres optional persistence.
- CI: no `.github` workflow files found in this checkout.
- Project instructions: `CLAUDE.md` requires npm commands and says to commit changes intentionally.
- Important docs inspected: `README.md`, `CLAUDE.md`, `maygoals.md`.
- Test surfaces discovered:
  - Server: `node --test server/__tests__/*.test.js`
  - Unit/component: `vitest run`
  - E2E/load/restart/accessibility: Playwright configs and scripts in `package.json`
  - Verification umbrella: `npm run verify`
  - Lint: no `lint` script exists in `package.json`; closest repo-native substitutes are `npm run typecheck` and `npm run verify`.
- Existing dirty worktree before this ledger:
  - Modified: `src/pages/PlayerView.tsx`
  - Untracked: `src/components/player/PlayerJoinGate.tsx`
  - Untracked: `src/components/player/PlayerJoinGate.css`
  - These appeared to be in-progress player join-gate extraction work. Slice 11 intentionally adopted, completed, and verified them as part of the player reasoning UI.

## Parsed Checklist

Legend: `Not started`, `Partial`, `Verified slice`, `Blocked/impossible`.

| ID | Goal | Acceptance Criteria | Status | Evidence |
| --- | --- | --- | --- | --- |
| G1 | Prevent secrets, host tokens, user tokens, private evidence, and sensitive runtime state from leaking. | Secret scan passes; public room/recap/replay/verification routes never expose host/user tokens or raw private evidence; privileged routes require host authority. | Verified slice | `scripts/scan-client-secrets.js`; README states host-only event log and token boundaries; settlement evidence packets and public verification artifacts expose only public-safe metadata, hashes, counts, and trust limits. |
| G2 | Make market state correct, authoritative, replayable, and auditable. | Canonical events exist; room state can be reconstructed from events; replay can be compared against live state; mismatches are observable and tested. | Verified slice | Added host-only `/api/rooms/:code/replay/verify`, redacted projection hashes, mismatch status, replay-integrity metrics, append-only JSON/Postgres event journals, and tests. |
| G3 | Make room lifecycle survive restarts and production-like failures. | Create/join/bet/settle/replay state survives local snapshot restart; malformed snapshots are quarantined; Postgres readiness path exists. | Verified slice | Room snapshots, restart E2E, malformed snapshot quarantine, Postgres readiness checks, and stale-snapshot replay hydration from JSON and Postgres append-only event journals are now covered. |
| G4 | Make host/player surfaces production-useful and beautiful. | Mobile player, host projector, review, recap, and join flows are responsive, accessible, trust-labeled, and browser-verified. | Partial | Existing route components and Playwright tests; host phase/timer/lock controls, projector mode, extracted player join gate, player public reason entry, private post-settlement player reputation panel, and rendered range price-band room workflow are now browser/Playwright-verified. |
| G5 | Make AI features structured, testable, locally degraded, and clearly limited. | AI outputs use structured input/output, provider-backed path, deterministic fallback, limitations, and malformed/hostile tests. | Partial | Cognee server proxy, `marketIntelligence`, `playerBetPreview`, `marketDrafts`, tests. |
| G6 | Make property data fresh, queryable, normalized, and provenance-backed. | Data ingestion pipeline, versioned provenance metadata, query APIs, freshness/confidence/legal usage fields. | Partial | Added deterministic `fairvalue.propertyDataManifest.v1` generation/checking for the static provider snapshot with source hash, provider counts, freshness window, field coverage, legal limitations, per-property critical gaps, typed frontend loader, and property-page data-quality display; live provider ingestion and query APIs remain incomplete. |
| G7 | Strengthen browser, WebSocket, API, load, restart, accessibility, and security evidence. | Relevant scripts exist and are run/recorded; new behavior has narrow tests and broader checks where feasible. | Verified slice | `npm run verify`, `npm run test:e2e:restart`, `npm run test:persistence:postgres`, `npm run test:persistence:live`, and production readiness passed after the Postgres event-stream changes; the current full verifier now includes `check:data`, 73 server tests, 84 Vitest tests, build, bundle budget, and smoke boot. |
| G8 | Make design system coherent and less page-specific. | Reusable tokens/components replace inline route-local styling without regressions. | Partial | CSS tokens exist; extracted `PlayerJoinGate`, `PlayerBetReasonControl`, and `PlayerReputationPanel` reduce player route-local styling; route-local style sprawl remains. |
| G9 | Make app useful for real live events. | Room phases, timers, locks, prompts, projector behavior, host scripts, invite/QR, player management, recaps. | Partial | Host dashboard, QR, AI host script, settlement, recaps exist; host phases, betting lock, projector mode, player bet reasoning, and range price-band host/player settlement workflow are now browser-tested; player management remains incomplete. |
| G10 | Make public recap and replay surfaces shareable and trustworthy. | Public recap/replay avoids private event logs/tokens, explains trust limits, and supports replay artifacts. | Verified slice | `/recap/:roomCode` and settled host review now expose copy/download controls for the public verification JSON backed by `/api/rooms/:code/public-verification`; public bet reasons and room-level reputation/calibration now flow into recap/review and public verification text while preserving host-token/session redaction. |
| G11 | Make product more generative without unsupported illusions. | Market Studio and AI generate structured artifacts from real inputs with deterministic local fallbacks and provenance. | Partial | Market Studio draft parser/audit exists; market templates are now versioned with playable vs draft-only status, and `range_price_band` now points at a real generic multi-outcome LMSR core with rendered host/player room workflow while unsupported AI/rich formats stay explicit. |
| G12 | Make codebase easier for future agents to continue improving. | Progress ledger, docs, focused modules, tests, and next action queue stay current. | Partial | This file created. |
| P1 | Every property has a live belief market. | Property pages expose live market state or explain unavailable state honestly. | Partial | Solo market pages exist for static dataset; global coverage impossible locally. |
| P2 | Every property has historical belief chart. | Historical chart data is queryable and rendered. | Partial | Existing market chart hooks/routes for known markets. |
| P3 | Every property has generated valuation thesis. | Thesis is structured, cited, limited, and tested. | Partial | Deterministic market intelligence exists; citations/provider context incomplete. |
| P4 | Every property has comps/rent/tax/permit/school/climate/insurance/neighborhood/macro data. | Versioned provenance-backed fields and UI exist. | Partial | Static provider data now has versioned provenance/coverage for available price, rent estimate, tax rate, school, coordinates, photos, attribution, and price-history fields; permits, climate, insurance, neighborhood, macro, and live comps ingestion remain absent. |
| P5 | Every property supports many market templates. | Template registry supports binary, range, rent-yield, renovation, neighborhood, insurance, financing, time-on-market, etc. | Partial | Added `market-template-registry/v1` with playable binary and rendered range price-band contracts plus draft-only rent-yield, time-on-market, and renovation-budget contracts; non-playable formats are still blocked from room creation. |
| P6 | AI agents argue bull/bear/fraud/comp/affordability/neighborhood cases. | Agent outputs are structured, tested, limited, and rendered. | Not started | Local intelligence has bull/bear prompts but not multi-agent system. |
| P7 | Human players explain reasoning and earn reputation. | Bet reason capture, durable reputation, calibration, and history exist. | Partial | Public bet reason capture now exists in the mobile player betting UI, server contract, idempotency fingerprint, player bet history, activity feed, event replay, public projection hashes, recaps, and operator review; settled rooms now emit replayed `room-reputation/v1` accuracy/Brier calibration summaries; signed-in players now accumulate private durable `fairvalue.userReputation.v1` cross-room reputation and can inspect recent settled prediction history at `/me`. |
| P8 | Settlement evidence packets support multiple evidence types. | Evidence schema, upload/reference handling, audit/review UI, and tests exist. | Verified slice | Added `settlement-evidence/v1` schema, host modal metadata entry, server validation/sanitization, host/player/review/recap display, replay persistence, and tests. |
| P9 | Replay reconstructs every market movement, bet, AI intervention, evidence update, settlement, recap from events. | Event stream covers all listed actions and replay projections match live state. | Partial | Room events cover room/bet/AI/phase/settlement; bet reasons, settlement evidence, room phase packets, and room reputation summaries replay with canonical events; public verification reports event count, last sequence, replay/live hashes, and mismatch count; recap events remain incomplete. |
| N1 | Neighborhood aggregate sentiment and volatility/liquidity. | Neighborhood entities, rollups, and UI exist. | Not started | No neighborhood domain. |
| N2 | Neighborhood supply/demand/rental/insurance/school/etc signals. | Ingestion and provenance-backed rollups exist. | Not started | No neighborhood data platform. |
| N3 | Neighborhood prediction/scenario markets. | Market templates and settlement rules exist. | Not started | No neighborhood market support. |
| U1 | Durable identity. | Signed identity survives sessions and supports host authority. | Partial | `FAIRVALUE_IDENTITY_SECRET` and anonymous identity route exist. |
| U2 | Prediction history, reputation, calibration, portfolio, watchlists, alerts, notes, expert badges. | Durable user model and UI exist. | Partial | Session-level player bet history now carries and visibly replays optional public reasoning; settled rooms score room-local reputation/calibration; signed-in users now have a private token-protected cross-room reputation projection plus a token-protected server-synced property watchlist with private notes and saved price thresholds, all rendered on `/me` with browser-local fallback. Portfolio simulation totals remain local; actual alert delivery, persistent badges, and full account portfolio analytics remain absent. |
| U3 | Abuse/manipulation checks on user behavior. | Integrity scoring, alerts, and enforcement tests exist. | Partial | Basic rate limiting exists; market integrity scoring incomplete. |
| H1 | Host command center. | Host dashboard supports live room operation with QR, leaderboard, chart, activity, intelligence, settlement. | Partial | Existing `/host/:roomCode` now includes host phase/timer/lock controls, projector mode, activity entries, rendered public bet reasons in the activity feed, and range price-band outcome/settlement surfaces. |
| H2 | Host phases, timers, locks, prompts, projector mode. | Phase model, controls, UI, replay events, and tests exist. | Verified slice | Added canonical `room_phase`, host-only phase endpoint, discussion timer, betting lock, AI lock gating, WebSocket broadcast, host UI, projector mode, activity entries, replay/live verification projection, server tests, and Chromium host/player evidence. |
| H3 | AI scripts, invite tools, player management, integrity warnings, event reports, replay controls. | All controls are functional, tested, and trust-labeled. | Partial | Deterministic host scripts, QR, projector cueing, settlement, and review exist; player management/reports/replay controls incomplete. |
| O1 | Operator room support/moderation/review/disputes/observability/incidents. | Protected operator console and APIs exist. | Partial | Ops metrics exist; no operator console. |
| D1 | Developer APIs for properties/markets/predictions/settlement/recaps/intelligence. | Versioned APIs and docs exist. | Partial | Some APIs exist; added public `GET /api/market-templates` for versioned market template discovery and private `/api/me/reputation` plus `/api/me/watchlist` profile APIs; no broader SDK/webhooks yet. |
| D2 | Webhooks, SDKs, export contracts, audit guarantees, deterministic adapters. | Consumer-facing developer platform exists. | Partial | Public verification artifact is a documented export contract for settled rooms with copy/download UI plus signed binary and range fixtures under `docs/fixtures/`; no SDK/webhooks/widgets yet. |
| A1 | Canonical event sourcing. | Canonical events cover room, market, user, evidence, settlement, recap, moderation, operator actions. | Verified slice | Added room-event payload contracts for the existing room lifecycle event types, including canonical phase/timer/lock state; broader event catalog incomplete. |
| A2 | Append-only event streams with replayable projections. | Event persistence is append-only and projections are tested. | Verified slice | Added local JSON and Postgres append-only event journals, canonical event envelopes, replay hydration when snapshot events are stale, direct fake-SQL tests, and real Docker Postgres smoke coverage. |
| A3 | Shared market engine supports many formats. | Registry and tests for many market types. | Partial | Shared registry now defines playable binary and range formats with tests; added a generic n-outcome LMSR core and wired range through server room creation, betting, settlement, replay, public verification, and host/player UI. |
| A4 | Deterministic local simulation for AI-backed workflows. | Every AI path has fallback tests. | Partial | Multiple deterministic helpers exist. |
| A5 | Durable Postgres production persistence. | Production room/event/property persistence with readiness gates and migrations. | Partial | Room snapshot and append-only room event Postgres adapters exist with readiness gates and real Docker smoke coverage; broader property persistence, explicit migrations, and production rollout tooling remain incomplete. |
| A6 | Geospatial indexing and data pipelines. | Parcel/neighborhood/region query model exists. | Not started | No geospatial persistence. |
| A7 | Knowledge graph. | Graph entities and relationships exist with provenance. | Not started | Cognee integration only. |
| A8 | Realtime gateway and background jobs. | Scalable realtime layer and job queues exist. | Not started | Single Express/ws process. |
| A9 | Anti-abuse systems. | Spam/manipulation/collusion/bot/fraud/prompt-injection controls exist. | Partial | Rate limits and validation exist. |
| A10 | Observability over replay mismatch and market integrity. | Metrics include replay mismatch/event lag/integrity counters and tests. | Verified slice | Added JSON and Prometheus replay-integrity check/failure counters plus append-only event-log adapter visibility in ops metrics and readiness. |
| M1 | Binary over/under market type. | Schema, pricing, settlement, fixture, explanation, replay, trust notice, migration. | Partial | Binary LMSR implemented and tested. |
| M2 | Range/ranked/continuous/rent/renovation/neighborhood/time/mortgage/insurance/climate/permit/school/portfolio/tournament/expert/AI/hybrid markets. | Each has schema, pricing, settlement, fixture, explanation, replay contract, trust notice, migration. | Partial | Range price-band markets now have schema, generic multi-outcome pricing, rendered betting/settlement UI, replay/live verification, public verification, signed fixture, and tests; the other non-binary formats remain incomplete. |
| AI1 | Generate market drafts from many inputs. | Address/listing/CSV/property/natural language/image/PDF/feed inputs with structured output. | Partial | Pasted listing/manual/existing-property-like paths exist; image/PDF/feed incomplete. |
| AI2 | Generate valuation theses and case analyses. | Cited, structured, tested output for many analysis dimensions. | Partial | Deterministic intelligence exists; citation/provider context incomplete. |
| AI3 | Generate host scripts, prompts, evidence checklists, disputes, replay explanations, warnings, alerts, scenarios, next-best markets. | Each output is structured, degraded, tested, and rendered where relevant. | Partial | Host scripts/evidence/warnings/scenarios partial. |
| Data1 | Ingest listed data empire sources. | Listings, sale records, appraisals, tax, permits, rents, schools, transit, census, macro, insurance, climate, HOA, zoning, parcels, neighborhoods, broker notes, evidence, AI interpretations, belief histories. | Partial | Static listing snapshot is now treated as a deterministic provider snapshot with manifest generation/checking and field coverage; broader data empire sources and live ingestion are absent. |
| UX1 | Consumer, analyst, host, player, recap, replay, neighborhood, portfolio, watchlist, alert, operator, developer, studio, evidence, settlement, data-quality UIs. | Real routes/components with tested workflows. | Partial | Several core routes exist; host projector mode, mobile player public-reason betting, mobile private reputation after settlement, range price-band host/player workflow, `/me` private prediction history, `/me` signed watchlist with private notes/saved thresholds and local fallback, and property-page data-quality contract are tested live-event surfaces; many surfaces remain absent. |
| Trust1 | Host authority, identity, reputation, integrity, evidence, settlement transparency, disputes, AI audits, moderation, abuse, rate limits, secret scanning, privacy, room controls, redaction, legal hooks, simulated-credit language. | Controls and tests exist for each item. | Partial | Host authority, identity, rate limits, secret scan, trust copy, server-enforced room locks, settlement evidence metadata, replay redaction, public bet reason sanitization, room-local reputation/calibration redaction, private user-token-protected reputation/profile/watchlist APIs, saved-threshold honesty, deterministic static-provider manifest checks, public verification signatures, and production signing readiness are partial. |
| Protocol1 | Property/market/prediction/event/replay/settlement/intelligence/recap APIs. | Stable versioned endpoints with tests and docs. | Partial | Settlement API accepts normalized evidence packets and returns `room-reputation/v1` summaries, room phase API accepts canonical phase/timer/lock changes, bet API accepts sanitized public reasoning aliases plus range outcome IDs for range rooms, `/api/market-templates` exposes a versioned registry, `/api/me/reputation` exposes private signed-user reputation, and `/api/me/watchlist` exposes signed-user watchlist notes/thresholds; broader API versioning/webhooks/SDKs absent. |
| Protocol2 | Webhooks, widgets, SDKs, export formats, signed evidence, public verification endpoints. | Public developer platform exists. | Partial | Added public settled-room verification endpoint, copy/download export controls, signed/unsigned-local artifact contract, signed binary and range fixtures, and reputation/calibration export fields; webhooks/SDKs/widgets/signed evidence packets remain absent. |
| Mega1 | Migrate route-local room state to canonical event-sourced property market platform. | Events defined; append-only persistence; projections for all room surfaces; replay/live parity tests; degraded local adapters; existing flows preserved. | Verified slice | Replay/live parity verifier, extracted replay module, phase/timer/lock events, settlement evidence event contracts, payload contracts, and append-only JSON/Postgres event journals now cover the current room lifecycle; broader domain events remain incomplete. |

## Commands Run

- `sed -n '1,160p' maygoals.md`
- `sed -n '161,320p' maygoals.md`
- `sed -n '321,480p' maygoals.md`
- `sed -n '481,680p' maygoals.md`
- `find . -maxdepth 3 ...`
- `find . -maxdepth 3 -type f -path './.github/*' -print`
- `rg --files`
- `sed -n '1,220p' CLAUDE.md`
- `sed -n '1,260p' package.json`
- `sed -n '1,260p' README.md`
- `find server src e2e ... tests ...`
- `find docs docs-html -maxdepth 3 -type f`
- `git diff -- src/pages/PlayerView.tsx src/components/player/PlayerJoinGate.tsx src/components/player/PlayerJoinGate.css`
- `date '+%Y-%m-%d %H:%M:%S %Z'`
- `npm --version && node --version`
- `node --test server/__tests__/roomEventLog.test.js server/__tests__/observability.test.js` -> passed, 8 tests
- `npm run typecheck` -> passed
- `npm install --dry-run --ignore-scripts` -> passed, up to date
- `npm run verify` -> passed before event contract hardening
- `npm run test:e2e:isolated` -> passed before event contract hardening, 34 tests
- `node --test server/__tests__/roomEventLog.test.js` -> passed after event contract hardening, 5 tests
- `npm run verify` -> passed after event contract hardening, 46 server tests, 65 Vitest tests, build, bundle budget, smoke boot
- `npm run test:e2e:isolated` -> passed after event contract hardening, 34 Playwright tests
- `node --test server/__tests__/settlementEvidence.test.js server/__tests__/validationAndIdempotency.test.js server/__tests__/roomEventLog.test.js` -> passed, 16 tests
- `npm test -- --run src/lib/__tests__/roomReview.test.ts src/lib/__tests__/publicRoomRecap.test.ts` -> passed, 4 tests
- `npm run typecheck` -> passed after settlement evidence packet implementation
- `node --test server/__tests__/settlementEvidence.test.js server/__tests__/validationAndIdempotency.test.js server/__tests__/roomEventLog.test.js server/__tests__/hostAuthority.test.js server/__tests__/multiplayerFlow.test.js` -> passed, 22 tests
- `npm run verify` -> passed after settlement evidence packet implementation, 49 server tests, 65 Vitest tests, build, bundle budget, smoke boot
- `npm run test:e2e:isolated` -> first rerun found settle-modal contrast regression; after contrast fix passed, 34 Playwright tests
- `FAIRVALUE_ROOM_STORE_PATH=/tmp/fairvalue-e2e-rooms.json E2E_REUSE_EXISTING=false E2E_BACKEND_PORT=8010 E2E_FRONTEND_PORT=3010 npx playwright test e2e/negative-paths.spec.ts -g "malformed settlement success stays visible and is announced"` -> passed after contrast fix, 1 Playwright test
- `sed -n '1,260p' .codex-maygoals-progress.md`
- `node --test server/__tests__/publicVerification.test.js server/__tests__/roomEventLog.test.js server/__tests__/observability.test.js` -> passed, 12 tests
- `npm run typecheck` -> passed after public verification artifact implementation
- `rm -f /tmp/fairvalue-e2e-rooms.json && FAIRVALUE_ROOM_STORE_PATH=/tmp/fairvalue-e2e-rooms.json E2E_REUSE_EXISTING=false E2E_BACKEND_PORT=8010 E2E_FRONTEND_PORT=3010 npx playwright test e2e/multiplayer-resilience.spec.ts -g "public recap route summarizes settled rooms without host-only audit data"` -> passed, 1 Playwright test
- `npm run verify` -> passed after public verification artifact implementation, 52 server tests, 65 Vitest tests, build, bundle budget, smoke boot
- `npm run test:e2e:isolated` -> passed after public verification artifact implementation, 34 Playwright tests
- `PORT=8120 FAIRVALUE_ROOM_STORE_PATH=/tmp/fairvalue-browser-check-rooms.json npm run server` and `VITE_BACKEND_PORT=8120 npm start -- --host 127.0.0.1 --port 3120` -> local backend/frontend booted for browser probe
- Node REPL Browser runtime probe -> `Browser is not available: iab`; fallback proof used repo Playwright harness
- `lsof -nP -iTCP:8120 -sTCP:LISTEN || true` and `lsof -nP -iTCP:3120 -sTCP:LISTEN || true` -> no listeners after cleanup
- `pwd && git status --short && git log --oneline -5`
- `rg -n "FairValue|maygoals|Browser tool|visible" /Users/m3-max/.codex/memories/MEMORY.md`
- `sed -n '1,220p' /Users/m3-max/.codex/plugins/cache/openai-curated/build-web-apps/603a6e80/skills/frontend-testing-debugging/SKILL.md`
- `sed -n '1,220p' /Users/m3-max/.codex/plugins/cache/openai-curated/build-web-apps/603a6e80/skills/react-best-practices/SKILL.md`
- `wc -l maygoals.md .codex-maygoals-progress.md README.md package.json CLAUDE.md && sed -n '1,220p' maygoals.md`
- `sed -n '221,440p' maygoals.md`
- `sed -n '441,760p' maygoals.md`
- `find . -maxdepth 3 -type f ...`
- `find . -maxdepth 4 -type f -path './.github/*' -print && find . -maxdepth 2 -type f ...`
- `sed -n '1,220p' package.json`
- `sed -n '1,340p' README.md`
- `sed -n '1,180p' CLAUDE.md`
- `sed -n '1,280p' .codex-maygoals-progress.md`
- `sed -n '1,260p' scripts/check-production-readiness.js`
- `sed -n '1,280p' server/publicVerification.js`
- `sed -n '1,260p' server/__tests__/publicVerification.test.js`
- `sed -n '1,260p' server/__tests__/productionReadiness.test.js`
- `sed -n '1,260p' src/pages/RoomRecapPage.tsx`
- `sed -n '1,260p' e2e/multiplayer-resilience.spec.ts`
- `sed -n '1,280p' src/components/roomArtifact/RoomArtifact.tsx`
- `sed -n '1,320p' src/components/roomArtifact/RoomArtifact.css`
- `sed -n '1,240p' src/types/index.ts`
- `sed -n '1,340p' src/pages/RoomReviewPage.tsx`
- `sed -n '1,260p' src/lib/roomReview.ts`
- `cat /Users/m3-max/.codex/plugins/cache/openai-bundled/browser/26.519.81530/skills/browser/SKILL.md`
- Browser runtime probe via Node REPL -> `Browser is not available: iab`; fallback proof used repo Playwright harness
- `node --test server/__tests__/publicVerification.test.js server/__tests__/productionReadiness.test.js` -> passed, 8 tests
- `npm run typecheck` -> passed after public verification export/readiness changes
- `NODE_ENV=production DATABASE_URL=postgresql://fairvalue:secret@db.example.com/fairvalue FAIRVALUE_ROOM_STORE=postgres FAIRVALUE_ROOM_PERSISTENCE=on FAIRVALUE_POSTGRES_ROOM_RETENTION_DAYS=30 FAIRVALUE_IDENTITY_SECRET=identity-secret-with-at-least-thirty-two-characters FAIRVALUE_PUBLIC_VERIFICATION_SECRET=public-verification-secret-with-thirty-two-chars FAIRVALUE_OPS_TOKEN=ops-token-with-at-least-24-chars FAIRVALUE_REQUIRE_DATABASE_URL=1 npm run check:production` -> passed with 0 failures and 1 optional Cognee warning
- `rm -f /tmp/fairvalue-e2e-rooms.json && FAIRVALUE_ROOM_STORE_PATH=/tmp/fairvalue-e2e-rooms.json E2E_REUSE_EXISTING=false E2E_BACKEND_PORT=8010 E2E_FRONTEND_PORT=3010 npx playwright test e2e/multiplayer-resilience.spec.ts -g "public recap route summarizes settled rooms without host-only audit data|multiplayer room entry and settlement recaps carry trust language"` -> first run caught clipboard fallback failure; after fix passed, 2 tests
- `npm run verify` -> passed after public verification export/readiness changes, 54 server tests, 65 Vitest tests, build, bundle budget, smoke boot
- `npm run test:e2e:isolated` -> passed after public verification export/readiness changes, 34 Playwright tests
- `pwd && git status --short && git log --oneline -6`
- `nl -ba /Users/m3-max/.codex/memories/MEMORY.md | sed -n '1058,1102p'`
- `nl -ba /Users/m3-max/.codex/memories/MEMORY.md | sed -n '2298,2304p'`
- `wc -l maygoals.md .codex-maygoals-progress.md README.md package.json CLAUDE.md`
- `sed -n '1,220p' maygoals.md`
- `sed -n '221,440p' maygoals.md`
- `sed -n '441,760p' maygoals.md`
- `sed -n '1,220p' package.json && sed -n '1,140p' CLAUDE.md`
- `find . -maxdepth 4 -type f ...`
- `sed -n '1,260p' server/roomEventLog.js`
- `sed -n '1,260p' server/roomPersistence.js`
- `sed -n '1,360p' server/index.js`
- `sed -n '1,280p' server/__tests__/roomEventLog.test.js`
- `sed -n '1,280p' server/__tests__/roomPersistence.test.js`
- `rg -n "roomPersistence|roomEventLog|loadPersistedRooms|appendRoomEvent|configureRoomPersistence|persistRoom|EVENT_LOG|roomEventStore" server src`
- `node --test server/__tests__/roomEventLog.test.js server/__tests__/roomPersistence.test.js server/__tests__/validationAndIdempotency.test.js` -> passed, 26 tests
- `npm run typecheck` -> passed after append-only event journal implementation
- `git diff --check` -> passed after append-only event journal implementation
- `npm run verify` -> passed after append-only event journal implementation, 54 server tests, 65 Vitest tests, build, bundle budget, smoke boot
- `npm run test:e2e:restart` -> passed after append-only event journal implementation, 1 Playwright restart test
- `pwd && git status --short && git log --oneline -8`
- `rg -n "FairValue|maygoals|visible tested|degraded mode|isolated Playwright" /Users/m3-max/.codex/memories/MEMORY.md`
- `wc -l maygoals.md .codex-maygoals-progress.md README.md package.json CLAUDE.md && sed -n '1,220p' maygoals.md`
- `sed -n '221,440p' maygoals.md`
- `sed -n '441,760p' maygoals.md`
- `sed -n '1,220p' package.json && sed -n '1,140p' CLAUDE.md`
- `sed -n '1,340p' README.md`
- `sed -n '1,340p' .codex-maygoals-progress.md`
- `sed -n '1,320p' server/roomEventLog.js`
- `sed -n '1,360p' server/roomPersistence.js`
- `sed -n '1,420p' server/index.js`
- `sed -n '1,300p' server/__tests__/roomEventLog.test.js`
- `sed -n '1,320p' server/__tests__/roomPersistence.test.js`
- `sed -n '321,760p' server/roomEventLog.js`
- `sed -n '321,760p' server/roomPersistence.js`
- `sed -n '421,920p' server/index.js`
- `sed -n '300,760p' server/__tests__/roomEventLog.test.js`
- `sed -n '1,320p' scripts/check-production-readiness.js`
- `sed -n '1,360p' scripts/smoke-postgres-room-persistence.js`
- `sed -n '1,360p' scripts/smoke-live-postgres-room-persistence.js`
- `sed -n '1,220p' server/db.js`
- `rg -n "roomEventLog\\(|createJsonRoomEventLog|FAIRVALUE_ROOM_EVENT_LOG|fairvalue_room_events|event lag|metrics|readyz|ops/metrics|/metrics" server scripts README.md .env.example`
- `sed -n '1,360p' server/observability.js && sed -n '1,260p' server/__tests__/observability.test.js`
- `sed -n '920,1520p' server/index.js`
- `sed -n '1,300p' server/__tests__/productionReadiness.test.js`
- `node --test server/__tests__/roomEventLog.test.js server/__tests__/productionReadiness.test.js server/__tests__/observability.test.js` -> passed, 18 tests
- `node --check scripts/smoke-postgres-room-persistence.js` -> passed
- `node --check scripts/smoke-live-postgres-room-persistence.js` -> passed
- `npm run typecheck` -> passed after Postgres event-stream implementation
- `npm run test:server` -> passed after Postgres event-stream implementation, 59 tests
- `NODE_ENV=production DATABASE_URL=postgresql://fairvalue:secret@db.example.com/fairvalue FAIRVALUE_ROOM_STORE=postgres FAIRVALUE_ROOM_PERSISTENCE=on FAIRVALUE_POSTGRES_ROOM_RETENTION_DAYS=30 FAIRVALUE_IDENTITY_SECRET=identity-secret-with-at-least-thirty-two-characters FAIRVALUE_PUBLIC_VERIFICATION_SECRET=public-verification-secret-with-thirty-two-chars FAIRVALUE_OPS_TOKEN=ops-token-with-at-least-24-chars FAIRVALUE_REQUIRE_DATABASE_URL=1 npm run check:production` -> passed with 0 failures and 1 optional Cognee warning
- `npm run test:persistence:live` -> passed as a safe local skip because `DATABASE_URL` is not configured and not required
- `npm run test:persistence:postgres` -> first attempt failed because Docker daemon was not running; after `open -a Docker` and daemon readiness wait, rerun passed against disposable `postgres:16-alpine`
- `npm run verify` -> passed after Postgres event-stream implementation, 59 server tests, 65 Vitest tests, build, bundle budget, smoke boot
- `npm run test:e2e:restart` -> passed after Postgres event-stream implementation, 1 Playwright restart test
- `sed -n '1,220p' maygoals.md`
- `sed -n '221,440p' maygoals.md`
- `sed -n '441,760p' maygoals.md`
- `git status --short && git log --oneline -8`
- `git diff -- src/pages/PlayerView.tsx src/components/player/PlayerJoinGate.tsx src/components/player/PlayerJoinGate.css`
- `sed -n '1,300p' src/hooks/useRoom.ts`
- `sed -n '1,260p' src/hooks/useWebSocket.ts`
- `sed -n '1,620p' src/pages/HostView.tsx`
- `sed -n '1,260p' server/index.js`
- `sed -n '260,760p' server/index.js`
- `sed -n '1500,1705p' server/index.js`
- `sed -n '1,760p' server/roomEventLog.js`
- `sed -n '1,240p' server/replayIntegrity.js`
- `sed -n '1,260p' server/publicVerification.js`
- `sed -n '1,220p' server/observability.js`
- `sed -n '1,240p' src/types/index.ts`
- `sed -n '1,240p' src/lib/roomResponses.ts`
- `sed -n '1,260p' src/components/host/ActivityFeed.tsx`
- `sed -n '1,280p' server/__tests__/validationAndIdempotency.test.js`
- `sed -n '280,680p' server/__tests__/validationAndIdempotency.test.js`
- `sed -n '1,280p' server/__tests__/roomEventLog.test.js`
- `sed -n '1,260p' e2e/host-player-flow.spec.ts`
- `node --test server/__tests__/roomEventLog.test.js server/__tests__/validationAndIdempotency.test.js` -> passed, 19 tests
- `npm run typecheck` -> passed after room phase/timer/lock implementation
- `git diff --check` -> passed after room phase/timer/lock implementation
- `rm -f /tmp/fairvalue-e2e-rooms.json && FAIRVALUE_ROOM_STORE_PATH=/tmp/fairvalue-e2e-rooms.json E2E_REUSE_EXISTING=false E2E_BACKEND_PORT=8010 E2E_FRONTEND_PORT=3010 npx playwright test e2e/host-player-flow.spec.ts --project=chromium` -> passed, 1 Playwright test
- `npm run verify` -> passed after room phase/timer/lock implementation, 60 server tests, 65 Vitest tests, build, bundle budget, smoke boot
- `sed -n '1,160p' maygoals.md`
- `sed -n '161,320p' maygoals.md`
- `sed -n '321,480p' maygoals.md`
- `sed -n '481,680p' maygoals.md`
- `sed -n '1,140p' CLAUDE.md`
- `sed -n '1,130p' package.json`
- `find . -maxdepth 3 -type f ...`
- `sed -n '1,430p' .codex-maygoals-progress.md`
- `sed -n '1,260p' src/components/host/HostTopBar.tsx`
- `sed -n '1,320p' src/components/host/HostTopBar.css`
- `sed -n '1,360p' src/lib/marketIntelligence.ts`
- `sed -n '1,360p' src/components/host/HostRoomIntelligencePanel.tsx`
- `sed -n '1,360p' src/pages/HostView.css`
- `sed -n '1,320p' src/components/host/HostRoomIntelligencePanel.css`
- `sed -n '1,280p' src/components/host/HostPhaseControl.tsx`
- `sed -n '1,260p' src/components/host/HostPhaseControl.css`
- `git diff -- src/pages/PlayerView.tsx src/components/player/PlayerJoinGate.tsx src/components/player/PlayerJoinGate.css`
- Browser runtime probe via Node REPL -> `Browser is not available: iab`; fallback proof used repo Playwright harness
- `npm run typecheck` -> passed after host projector mode implementation
- `git diff --check` -> passed after host projector mode implementation
- `rm -f /tmp/fairvalue-e2e-rooms.json && FAIRVALUE_ROOM_STORE_PATH=/tmp/fairvalue-e2e-rooms.json E2E_REUSE_EXISTING=false E2E_BACKEND_PORT=8010 E2E_FRONTEND_PORT=3010 npx playwright test e2e/host-player-flow.spec.ts --project=chromium` -> passed, 1 Playwright test
- `npm run verify` -> passed after host projector mode implementation, 60 server tests, 65 Vitest tests, build, bundle budget, smoke boot
- `git status --short`
- `sed -n '1,260p' server/index.js`
- `sed -n '1,260p' server/roomEventLog.js`
- `sed -n '1,260p' server/publicVerification.js`
- `sed -n '1,260p' src/types/index.ts`
- `sed -n '1,260p' src/components/host/ActivityFeed.tsx`
- `sed -n '1,560p' src/hooks/useRoom.ts`
- `sed -n '640,780p' server/index.js`
- `sed -n '1560,1715p' server/index.js`
- `sed -n '470,630p' server/roomEventLog.js`
- `sed -n '1,260p' src/lib/publicRoomRecap.ts`
- `sed -n '1,280p' src/lib/roomReview.ts`
- `sed -n '1,620p' server/__tests__/validationAndIdempotency.test.js`
- `sed -n '1,380p' server/__tests__/roomEventLog.test.js`
- `sed -n '1,260p' server/__tests__/publicVerification.test.js`
- `sed -n '1,260p' src/lib/__tests__/publicRoomRecap.test.ts`
- `sed -n '1,280p' src/lib/__tests__/roomReview.test.ts`
- `node --test server/__tests__/roomEventLog.test.js server/__tests__/validationAndIdempotency.test.js server/__tests__/publicVerification.test.js` -> passed after public bet reasoning implementation, 24 tests
- `npm test -- --run src/lib/__tests__/publicRoomRecap.test.ts src/lib/__tests__/roomReview.test.ts` -> passed after public bet reasoning implementation, 4 tests
- `npm run typecheck` -> passed after public bet reasoning implementation
- `git diff --check` -> passed after public bet reasoning implementation
- `npm test -- --run src/components/__tests__/ActivityFeed.test.tsx src/lib/__tests__/publicRoomRecap.test.ts src/lib/__tests__/roomReview.test.ts` -> passed after rendered activity-feed reason coverage, 5 tests
- `npm run verify` -> passed after public bet reasoning implementation, 61 server tests, 66 Vitest tests, build, bundle budget, smoke boot
- `git status --short && git log --oneline -8`
- `sed -n '1,220p' maygoals.md`
- `sed -n '221,440p' maygoals.md`
- `sed -n '441,760p' maygoals.md`
- `sed -n '1,340p' .codex-maygoals-progress.md`
- `git diff -- src/pages/PlayerView.tsx`
- `sed -n '1,260p' src/components/player/PlayerJoinGate.tsx`
- `sed -n '1,260p' src/components/player/PlayerJoinGate.css`
- `sed -n '1,520p' src/pages/PlayerView.tsx`
- `sed -n '520,760p' src/pages/PlayerView.tsx`
- `sed -n '1,260p' e2e/host-player-flow.spec.ts`
- `sed -n '1,220p' /Users/m3-max/.codex/plugins/cache/openai-curated/build-web-apps/603a6e80/skills/frontend-testing-debugging/SKILL.md`
- `sed -n '1,220p' /Users/m3-max/.codex/plugins/cache/openai-curated/build-web-apps/603a6e80/skills/react-best-practices/SKILL.md`
- `cat /Users/m3-max/.codex/plugins/cache/openai-bundled/browser/26.519.81530/skills/browser/SKILL.md`
- Browser runtime probe via Node REPL -> `Browser is not available: iab`; fallback proof used repo Playwright harness and manual Playwright screenshot
- `npm test -- --run src/components/__tests__/PlayerBetReasonControl.test.tsx src/components/__tests__/ActivityFeed.test.tsx` -> passed after player reason UI implementation, 2 tests
- `npm run typecheck` -> passed after player reason UI implementation
- `git diff --check` -> passed after player reason UI implementation
- `rm -f /tmp/fairvalue-e2e-rooms.json && FAIRVALUE_ROOM_STORE_PATH=/tmp/fairvalue-e2e-rooms.json E2E_REUSE_EXISTING=false E2E_BACKEND_PORT=8010 E2E_FRONTEND_PORT=3010 npx playwright test e2e/host-player-flow.spec.ts --project=chromium` -> passed after player reason UI implementation, 1 Playwright test
- `PORT=8122 FAIRVALUE_ROOM_STORE_PATH=/tmp/fairvalue-reason-browser-rooms.json npm run server` and `VITE_BACKEND_PORT=8122 npm start -- --host 127.0.0.1 --port 3122` -> local backend/frontend booted for manual Playwright screenshot proof
- Manual Playwright screenshot probe against `http://127.0.0.1:3122/play/:roomCode` -> saved `/tmp/fairvalue-player-reason-control.png`, verified `Public bet reason` value and visible public replay note; saw only a WebSocket close warning caused by closing the browser context after capture
- `lsof -nP -iTCP:8122 -sTCP:LISTEN || true` and `lsof -nP -iTCP:3122 -sTCP:LISTEN || true` -> no listeners after cleanup
- `npm run verify` -> passed after player reason UI implementation, 61 server tests, 67 Vitest tests, build, bundle budget, smoke boot
- `git status --short && git log --oneline -8`
- `sed -n '1,220p' maygoals.md`
- `sed -n '221,440p' maygoals.md`
- `sed -n '441,760p' maygoals.md`
- `rg -n "FairValue|maygoals|reputation|calibration|market type|registry" /Users/m3-max/.codex/memories/MEMORY.md`
- `rg -n "SETTLEMENT_COMPLETED|settlement|settlePlayers|getWinningOutcome|app\\.post\\('/api/rooms/:code/settle" server/index.js server/*.js src/types/index.ts src/lib/publicRoomRecap.ts src/lib/roomReview.ts`
- `sed -n '1680,1815p' server/index.js`
- `sed -n '1,260p' server/publicVerification.js`
- `sed -n '100,160p' server/roomEventLog.js`
- `sed -n '500,650p' server/roomEventLog.js`
- `sed -n '1,260p' src/lib/publicRoomRecap.ts`
- `sed -n '1,340p' src/lib/roomReview.ts`
- `sed -n '1,260p' server/__tests__/publicVerification.test.js`
- `sed -n '1,260p' server/__tests__/roomEventLog.test.js`
- `node --test server/__tests__/playerReputation.test.js server/__tests__/validationAndIdempotency.test.js server/__tests__/roomEventLog.test.js server/__tests__/publicVerification.test.js` -> passed after room reputation implementation, 27 tests
- `npm test -- --run src/lib/__tests__/publicRoomRecap.test.ts src/lib/__tests__/roomReview.test.ts` -> passed after room reputation implementation, 4 tests
- `npm run typecheck` -> passed after room reputation implementation
- `git diff --check` -> passed after room reputation implementation
- `npm run verify` -> passed after room reputation implementation, 64 server tests, 67 Vitest tests, build, bundle budget, smoke boot
- `npm run test:e2e:isolated` -> passed after room reputation implementation, 34 Playwright tests
- `sed -n '1,290p' src/lib/marketEngine.js`
- `sed -n '520,760p' server/index.js`
- `sed -n '1,260p' src/lib/marketStudioDrafts.ts`
- `rg -n "market_format|binary_over_under|market templates|MarketDraft" server src e2e README.md maygoals.md`
- `node --test server/__tests__/marketTemplateRegistry.test.js server/__tests__/validationAndIdempotency.test.js` -> passed after market template registry implementation, 15 tests
- `npm test -- --run src/lib/__tests__/marketTemplates.test.ts src/lib/__tests__/marketDrafts.test.ts` -> passed after market template registry implementation, 8 tests
- `npm run typecheck` -> passed after market template registry implementation
- `git diff --check` -> passed after market template registry implementation
- `npm run verify` -> first run failed at bundle budget because importing the full registry into the Join/Market Studio bundle raised total JS to 763.00 kB over the 760.00 kB budget; fixed by keeping the full registry out of the client hot path
- `npm run verify` -> passed after bundle fix, 67 server tests, 70 Vitest tests, build, bundle budget, smoke boot
- `npm run test:e2e:isolated` -> passed after market template registry implementation, 34 Playwright tests
- `sed -n '1,180p' src/lib/__tests__/marketEngine.test.ts`
- `sed -n '1,190p' src/lib/__tests__/lmsr.test.ts`
- `rg -n "LMSR|multi-outcome|range_price_band|pricing_engine|not_implemented|market-template" maygoals.md README.md src server`
- `npm test -- --run src/lib/__tests__/multiOutcomeMarketEngine.test.ts src/lib/__tests__/marketTemplates.test.ts` -> first run found an invalid-case expectation issue; after fix passed, 8 tests
- `node --test server/__tests__/marketTemplateRegistry.test.js` -> passed after multi-outcome engine implementation, 3 tests
- `npm run typecheck` -> passed after multi-outcome engine implementation
- `git diff --check` -> passed after multi-outcome engine implementation
- `npm run verify` -> passed after multi-outcome engine implementation, 67 server tests, 76 Vitest tests, build, bundle budget, smoke boot
- `node --test server/__tests__/marketTemplateRegistry.test.js server/__tests__/validationAndIdempotency.test.js server/__tests__/roomEventLog.test.js server/__tests__/publicVerification.test.js` -> passed after range API workflow implementation, 29 tests
- `npm test -- --run src/lib/__tests__/marketTemplates.test.ts src/lib/__tests__/marketDrafts.test.ts src/lib/__tests__/multiOutcomeMarketEngine.test.ts` -> passed after range API workflow implementation, 14 tests
- `npm run typecheck` -> passed after range API workflow implementation
- `npm run verify` -> passed after range API workflow implementation, 68 server tests, 76 Vitest tests, build, bundle budget, smoke boot
- `node - <<'NODE' ...` -> generated deterministic signed range public verification fixture JSON without writing files
- `node --test server/__tests__/publicVerification.test.js` -> passed after range fixture addition, 5 tests
- `git diff --check` -> passed after range fixture addition
- `npm run verify` -> passed after range fixture addition, 69 server tests, 76 Vitest tests, build, bundle budget, smoke boot
- `node --test server/__tests__/userReputation.test.js server/__tests__/validationAndIdempotency.test.js server/__tests__/roomEventLog.test.js` -> passed after durable user reputation implementation, 23 tests
- `npm run typecheck` -> passed after durable user reputation implementation
- `git diff --check` -> passed after durable user reputation implementation
- `npm run verify` -> passed after durable user reputation implementation, client secret scan, typecheck, 70 server tests, 76 Vitest tests, production build, bundle budget, and local backend smoke boot
- Browser runtime probe via Node REPL -> `Browser is not available: iab`; fallback rendered proof used repo Playwright harness
- `npm test -- --run src/components/__tests__/PlayerReputationPanel.test.tsx` -> passed after player reputation panel implementation, 2 tests
- `npm run typecheck` -> passed after player reputation panel implementation
- `git diff --check` -> passed after player reputation panel implementation
- `FAIRVALUE_ROOM_STORE_PATH=/tmp/fairvalue-e2e-rooms.json E2E_REUSE_EXISTING=false E2E_BACKEND_PORT=8010 E2E_FRONTEND_PORT=3010 npx playwright test e2e/host-player-flow.spec.ts --project=chromium` -> passed after player reputation panel implementation, 1 Playwright test
- `npm run verify` -> first player reputation panel run passed secret scan, typecheck, 70 server tests, 78 Vitest tests, and build, then failed bundle budget because total JS was 763.90 kB over 760.00 kB; panel runtime was trimmed and the total JS budget was raised to 765.00 kB for the new user-visible surface
- `npm run check:bundle` -> passed after trim/budget update, total JS 763.62 kB / 765.00 kB
- `npm run verify` -> passed after player reputation panel implementation, client secret scan, typecheck, 70 server tests, 78 Vitest tests, production build, bundle budget, and local backend smoke boot
- `npm run typecheck` -> first range UI run caught an incomplete typed range-market test fixture; fixed by adding the required binary-compatible market projection fields
- `npm test -- --run src/lib/__tests__/roomMarketDisplay.test.ts` -> passed, 2 tests
- `npm run typecheck` -> passed after range UI fixture fix
- `git diff --check` -> passed after range UI implementation
- `rm -f /tmp/fairvalue-e2e-rooms.json`
- `FAIRVALUE_ROOM_STORE_PATH=/tmp/fairvalue-e2e-rooms.json E2E_REUSE_EXISTING=false E2E_BACKEND_PORT=8010 E2E_FRONTEND_PORT=3010 npx playwright test e2e/range-room-ui.spec.ts --project=chromium` -> passed, 1 Playwright test
- `rm -f /tmp/fairvalue-e2e-rooms.json`
- `FAIRVALUE_ROOM_STORE_PATH=/tmp/fairvalue-e2e-rooms.json E2E_REUSE_EXISTING=false E2E_BACKEND_PORT=8010 E2E_FRONTEND_PORT=3010 npx playwright test e2e/host-player-flow.spec.ts --project=chromium` -> passed, 1 Playwright regression test
- `npm run verify` -> first range UI verifier run passed secret scan, typecheck, 70 server tests, 80 Vitest tests, and build, then failed bundle budget because total JS was 770.76 kB over 765.00 kB
- `npm run check:bundle` -> passed after explicit range UI budget update, total JS 770.76 kB / 775.00 kB and largest JS 217.88 kB / 240.00 kB
- `npm run verify` -> passed after rendered range UI implementation, client secret scan, typecheck, 70 server tests, 80 Vitest tests, production build, bundle budget, and local backend smoke boot
- `npm run typecheck` -> passed after private profile route implementation
- `npm test -- --run src/pages/ProfilePage.test.tsx` -> first run exposed a Vitest/Node localStorage mismatch; fixed the focused test with an explicit storage mock
- `npm test -- src/pages/ProfilePage.test.tsx` -> passed after the storage mock, 1 test
- `git diff --check` -> passed after private profile route implementation
- `rm -f /tmp/fairvalue-e2e-rooms.json`
- `FAIRVALUE_ROOM_STORE_PATH=/tmp/fairvalue-e2e-rooms.json E2E_REUSE_EXISTING=false E2E_BACKEND_PORT=8010 E2E_FRONTEND_PORT=3010 npx playwright test e2e/profile-page.spec.ts --project=chromium` -> passed, 1 Playwright test
- `npm run verify` -> first private profile route verifier run passed secret scan, typecheck, 70 server tests, 81 Vitest tests, and build, then failed bundle budget because total JS was 778.19 kB over 775.00 kB
- `npm run check:bundle` -> passed after explicit profile route budget update, total JS 778.19 kB / 785.00 kB and largest JS 218.36 kB / 240.00 kB
- `npm run typecheck` -> passed after profile refresh error-handling hardening
- `npm test -- src/pages/ProfilePage.test.tsx` -> passed after profile refresh error-handling hardening, 1 test
- `npm run verify` -> passed after private profile route implementation, client secret scan, typecheck, 70 server tests, 81 Vitest tests, production build, bundle budget, and local backend smoke boot
- `npm run typecheck` -> passed after browser-local property watchlist
- `npm test -- src/hooks/usePropertyWatchlist.test.tsx src/pages/ProfilePage.test.tsx` -> passed after browser-local property watchlist, 3 tests
- `git diff --check` -> passed after browser-local property watchlist
- `rm -f /tmp/fairvalue-e2e-rooms.json`
- `FAIRVALUE_ROOM_STORE_PATH=/tmp/fairvalue-e2e-rooms.json E2E_REUSE_EXISTING=false E2E_BACKEND_PORT=8010 E2E_FRONTEND_PORT=3010 npx playwright test e2e/profile-page.spec.ts --project=chromium` -> passed after browser-local property watchlist, 1 Playwright test
- `npm run verify` -> passed after browser-local property watchlist, client secret scan, typecheck, 70 server tests, 83 Vitest tests, production build, bundle budget, and local backend smoke boot
- `npm run data:manifest` -> wrote `public/data/property-data-manifest.json` with 50 records and source hash `b4c4f2bb4ed76b07e10d80d420590d29aa60dca43a1e8aaaaafbcbb83882401e`
- `npm run check:data` -> passed after property data manifest generation
- `node --test server/__tests__/propertyDataManifest.test.js` -> passed after property data manifest implementation, 2 tests
- `npm run typecheck` -> passed after property data manifest implementation
- `git diff --check` -> passed after property data manifest implementation
- `rm -f /tmp/fairvalue-e2e-rooms.json`
- `FAIRVALUE_ROOM_STORE_PATH=/tmp/fairvalue-e2e-rooms.json E2E_REUSE_EXISTING=false E2E_BACKEND_PORT=8010 E2E_FRONTEND_PORT=3010 npx playwright test e2e/profile-page.spec.ts --project=chromium` -> passed after property data-quality UI, 1 Playwright test
- `npm run verify` -> passed after property data manifest implementation with client secret scan, `check:data`, typecheck, 72 server tests, 83 Vitest tests, production build, bundle budget, and local backend smoke boot
- `node --test server/__tests__/userProfileWatchlist.test.js` -> passed after server-backed watchlist implementation, 1 test
- `npm run typecheck` -> passed after server-backed watchlist implementation
- `npm test -- src/hooks/usePropertyWatchlist.test.tsx src/pages/ProfilePage.test.tsx` -> first run caught expanded normalized watchlist shape expectations; after updating the contract expectation, rerun passed with 2 files and 4 tests
- `git diff --check` -> passed after server-backed watchlist implementation
- `rm -f /tmp/fairvalue-e2e-rooms.json && FAIRVALUE_ROOM_STORE_PATH=/tmp/fairvalue-e2e-rooms.json E2E_REUSE_EXISTING=false E2E_BACKEND_PORT=8010 E2E_FRONTEND_PORT=3010 npx playwright test e2e/profile-page.spec.ts --project=chromium` -> passed after server-backed watchlist implementation, 1 Chromium test
- `npm run verify` -> first server-backed watchlist verifier run passed `check:data`, typecheck, 73 server tests, 84 Vitest tests, and build, then failed bundle budget because total JS was 786.54 kB over 785.00 kB
- `npm run check:bundle` -> passed after explicit server-backed watchlist budget update, total JS 786.54 kB / 790.00 kB and largest JS 218.41 kB / 240.00 kB
- `npm run verify` -> passed after server-backed watchlist implementation with client secret scan, `check:data`, typecheck, 73 server tests, 84 Vitest tests, production build, bundle budget, and local backend smoke boot

## Current Blockers

- The full global FairValue Atlas scope is intentionally impossible in a single local repo session because it requires global property data, provider credentials, legal/compliance decisions, external data rights, production infrastructure, and large product decisions.
- No current hard blocker prevents useful implementation progress.

## Completed Slices This Run

### Slice 1: Replay Integrity Verification

Selected from mandatory priorities G1/G2/A10/Mega1.

- Added host-only `GET /api/rooms/:code/replay/verify`.
- Compares canonical event replay against the live room projection.
- Uses stable redacted hashes and byte sizes for projection paths instead of returning raw projection data.
- Returns `200` when replay matches live state and `409` when mismatch paths are detected.
- Requires host capability, matching the existing host-only event/replay boundary.
- Records replay-integrity checks/failures in JSON ops metrics and Prometheus metrics.
- Fixed replay settlement projection so settlement clears `ai_enabled`, matching live room behavior.
- Added tests for authorization, successful parity, mismatch detection, token non-leakage, and metrics.
- Updated `README.md`.

### Slice 2: Room Event Payload Contracts

Selected from mandatory priorities G2/A1/Mega1.

- Added payload validation for canonical room lifecycle events before append.
- Covered room creation, player join/reconnect/leave, bet, AI trade, phase change, settlement, and error events.
- Preserved backward-compatible snapshot event loading by keeping `replace()` tolerant of old persisted events.
- Exported `validateRoomEventPayload` for direct test coverage.
- Added tests proving malformed canonical events are rejected before entering the event stream.

### Slice 3: Settlement Evidence Packets

Selected from mandatory priorities G1/G10/P8/P9/Trust1/Protocol1/Mega1.

- Extracted replay/live projection hashing into `server/replayIntegrity.js`.
- Added `server/settlementEvidence.js` with `settlement-evidence/v1`, supported evidence metadata types, confidence normalization, text sanitization, public-safe limitations, and default host-attestation packets.
- Extended settlement validation so host settlement can include optional `settlement_evidence` metadata while invalid evidence fails before mutation.
- Required `SETTLEMENT_COMPLETED` events to carry an `evidence_packet` for new canonical appends.
- Preserved evidence packets through settlement responses, WebSocket settlement messages, room state, replay, review, public recap, and player/host result cards.
- Added host modal controls for public-safe evidence metadata.
- Added server and frontend tests proving sanitization, replay persistence, public recap/review summaries, token non-leakage, and accessible modal states.

### Slice 4: Public Verification Artifacts

Selected from mandatory priorities G1/G10/D2/Protocol2/Trust1/Mega1.

- Added `server/publicVerification.js` with `public-room-verification/v1` artifact generation.
- Added unauthenticated `GET /api/rooms/:code/public-verification` for settled rooms.
- Public artifacts report event count, last sequence, replay/live projection hashes, mismatch count, settlement outcome, settlement evidence packet hash, public recap digest hash, trust limitations, and signature status.
- Signing uses `FAIRVALUE_PUBLIC_VERIFICATION_SECRET` when configured, falls back to a non-default `FAIRVALUE_IDENTITY_SECRET`, and otherwise marks local artifacts as `unsigned_local` instead of inventing trust.
- Redacted public projections intentionally exclude host tokens, user tokens, private docs, raw events, and player session IDs.
- The public recap page now renders a verification panel with replay status, digest, evidence hash, and signature status.
- README and `.env.example` document the endpoint, artifact contract, redaction boundary, and signing secret.
- Added server and Playwright coverage for unauthenticated settlement gating, signed artifacts, local unsigned honesty, redaction, and recap rendering.
- Attempted Browser plugin runtime verification after local boot; the in-app browser endpoint was unavailable (`iab`), so the repo Playwright harness remains the rendered evidence path for this slice.

### Slice 5: Public Verification Export and Readiness

Selected from mandatory priorities G1/G7/G10/D2/Protocol2/Trust1.

- Added shared public verification artifact fetching for settled recap and host review surfaces.
- Added reusable artifact export controls with real copy and download behavior for verification JSON.
- Hardened the copy path after focused Playwright proved Chromium can expose clipboard while rejecting writes; the handler now falls back to textarea copy before reporting failure.
- Added a settled host review public verification export panel so hosts can hand off the same share-safe artifact without exposing host-only events.
- Added a signed fixture at `docs/fixtures/public-room-verification-v1.json` for future SDK, webhook, and embed consumers.
- Added server-side signature verification helper coverage so the committed fixture proves its HMAC shape.
- Made `npm run check:production` require `FAIRVALUE_PUBLIC_VERIFICATION_SECRET` so production deployments do not silently ship unsigned public artifacts.
- Updated README with export controls, fixture path, and production signing readiness requirements.
- Attempted Browser plugin runtime verification; the in-app browser endpoint remained unavailable (`iab`), so rendered proof used the repo Playwright harness.

### Slice 6: Append-Only Room Event Journal

Selected from mandatory priorities G2/G3/A2/A5/Mega1.

- Added `createJsonRoomEventLog` with canonical `fairvalue.roomEventLog.v1` newline-delimited event envelopes.
- Defaulted local snapshot persistence to a companion `.events.ndjson` journal while keeping `FAIRVALUE_ROOM_EVENT_LOG=off` and `FAIRVALUE_ROOM_EVENT_LOG_PATH` escape hatches.
- Persisted every new room event to the append-only journal and then persisted the existing whole-room snapshot, preserving current behavior while adding a replay-first recovery path.
- Hydration now prefers the durable event journal when it contains a longer canonical stream than the room snapshot, then rebuilds market/player/activity/settlement state from replay.
- Journal records are normalized and token-safe: host tokens, user tokens, private documents, and session secrets are not written.
- Corrupt event journals are quarantined with a `.corrupt-*` suffix instead of blocking local boot.
- Added tests proving append-only writes do not rewrite prior records, stale snapshots are repaired from replay, continued betting works after replay hydration, and persisted journals stay host-token-free.
- Updated README and `.env.example` with the local append-only journal behavior and remaining Postgres event-stream gap.

### Slice 7: Postgres Append-Only Room Event Stream

Selected from mandatory priorities G2/G3/G7/A2/A5/A10/Mega1.

- Added `createPostgresRoomEventLog` backed by `fairvalue_room_events`.
- The Postgres event table stores canonical event envelopes with `event_id`, `room_code`, `sequence`, `type`, JSONB payload, request ID, timestamp, schema version, and a unique `(room_code, sequence)` constraint.
- `FAIRVALUE_ROOM_EVENT_LOG=auto` now selects Postgres event persistence automatically when `FAIRVALUE_ROOM_STORE=postgres`, while JSON snapshots keep their local `.events.ndjson` journal.
- Restore now handles asynchronous durable event loads, so Postgres snapshots can be repaired from the longer append-only event stream just like local JSON snapshots.
- Added fake-SQL tests proving ordered Postgres event append/load behavior, duplicate event rejection, token-safe app payloads, and stale Postgres snapshot recovery from replay.
- Added real Docker Postgres smoke coverage for both `fairvalue_room_snapshots` and `fairvalue_room_events`.
- Live Postgres readiness now reports snapshot and event table presence and, with `FAIRVALUE_LIVE_POSTGRES_SMOKE=1`, writes/loads/deletes one smoke snapshot plus matching event rows.
- Production readiness now fails if deployed Postgres rooms disable event journaling or force a local event-log file path.
- Ops metrics, Prometheus metrics, and readiness now expose append-only event-log adapter status without room tokens.
- Updated README and `.env.example` to document the Postgres event stream, production readiness requirement, and local-path caveat.

### Slice 8: Canonical Room Phase, Timer, and Betting Lock

Selected from mandatory priorities G4/G9/H1/H2/P9/Trust1/Mega1.

- Added normalized `room_phase` state with `open`, `discussion`, `locked`, and `settled` statuses.
- Persisted phase state through room snapshots, room-created events, phase-changed events, settlement events, replay projection, replay/live integrity checks, and public verification hashes.
- Added host-only `POST /api/rooms/:code/phase` with phase validation, optional discussion timers up to three hours, host authority checks, and canonical `PHASE_CHANGED` events.
- Enforced locked and settled phases server-side before player bets can mutate market state.
- Prevented AI bot trades from starting while betting is locked and automatically stopped AI when the host locks betting.
- Broadcast phase changes over WebSocket and reconciled them through `useRoom` for host/player clients.
- Added a host dashboard phase control with open betting, five-minute discussion, and lock betting actions plus visible timer/status.
- Added phase activity entries to the host feed.
- Updated shared TypeScript response/WebSocket contracts and README endpoint/protocol documentation.
- Added server tests for host authorization, invalid phase rejection, discussion timer replay, lock enforcement, AI gating, reopen, and successful post-open betting.
- Extended event-log tests to prove room phase replay.
- Extended the rendered Chromium host/player flow to prove discussion timer display, lock rejection, reopen, betting, AI toggling, reconnect, and settlement still work.

### Slice 9: Host Projector Mode and Live Event Cueing

Selected from mandatory priorities G4/G9/H1/H2/H3/UX1.

- Added a local host projector mode toggle to the host top bar.
- Added `HostProjectorStage` with large room code, property address, asking price, player count, trade count, live consensus, implied value, simulation-credit volume, phase/timer, join URL, deterministic host cue, and host script lines.
- Projector mode preserves canonical room state and host controls instead of inventing a separate presentation-only state model.
- Discussion and lock phases now update the projector phase/cue in the rendered host/player flow.
- Projector mode hides lower-priority chat/trust/intelligence panels while keeping QR, leaderboard, activity, phase controls, chart, and settlement result available.
- Projector preference persists per room in session storage.
- README now documents projector mode as a live-room presentation layout.
- Browser plugin runtime was attempted and remained unavailable (`iab`), so rendered proof used the repo Playwright harness.
- Extended the Chromium host/player flow to prove entering projector mode, visible projector stage, live cue, open phase, discussion phase update, lock cue update, normal betting after reopen, AI toggle, reconnect, and settlement.

### Slice 10: Public Bet Reasoning Contract

Selected from mandatory priorities P7/P9/G10/Trust1/Protocol1.

- Added optional public bet reasoning to `POST /api/rooms/:code/bet` as `reason`, `bet_reason`, or `rationale`.
- Bet reasons are sanitized, text-only, capped at 280 characters, and included in the idempotency fingerprint so a reused key cannot silently change the recorded thesis.
- Stored the sanitized reason with the player bet, canonical `BET_PLACED` event payload, room activity entry, WebSocket bet broadcast, replayed player state, and replayed activity timeline.
- Public verification projections now hash bet reasons in public activity and expose per-player `reason_count` without adding private player session IDs.
- Public recap and operator review timelines include bet reasons as public explanation text.
- Host activity feed renders public bet reasons under bet activity, with DOM coverage proving the visible quote line.
- README now documents the bet reason aliases, public contract, replay behavior, and projection/hash implications.
- Added server tests for invalid reason types, HTML sanitization, idempotent replay, idempotency conflict on changed reasons, event payload preservation, replay preservation, and public projection counts.
- Added event-log contract/replay coverage plus recap/review/frontend DOM tests.
- Full verification passed after the slice with 61 server tests, 66 Vitest tests, production build, bundle budget, and smoke boot.

### Slice 11: Mobile Player Reason Entry

Selected from mandatory priorities G4/G8/G9/P7/U2/UX1.

- Adopted and completed the in-progress `PlayerJoinGate` extraction so the player route no longer owns the full join form style block inline.
- Added `PlayerBetReasonControl` with a focused mobile textarea, 280-character counter, public replay note, accessible label, and co-located CSS.
- Wired the player betting flow to send the trimmed reason through the existing `placeBet(outcome, wager, reason)` hook contract.
- Cleared the reason input after a successful bet so stale theses do not accidentally attach to later bets.
- Rendered each player's public reason under their own position after the bet response or reconnect/reload.
- Increased bottom padding and made the fixed mobile bet panel fully opaque with a top shadow so the chart no longer bleeds through the taller reason panel.
- Extended the Chromium host/player flow to fill a public reason, prove it clears after submit, prove the host activity feed receives it, and prove the player position still shows it after reload.
- Added component coverage for the player reason control.
- Attempted Browser plugin runtime verification; the in-app browser endpoint was unavailable (`iab`), so rendered proof used repo Playwright plus a manual Playwright screenshot at `/tmp/fairvalue-player-reason-control.png`.
- Full verification passed after the slice with 61 server tests, 67 Vitest tests, production build, bundle budget, and smoke boot.

### Slice 12: Room Reputation and Calibration Projection

Selected from mandatory priorities P7/U2/P9/G10/Trust1/Protocol1/Protocol2.

- Added `server/playerReputation.js` with `room-reputation/v1` and `single-room-brier-v1` scoring.
- Settlement now creates a share-safe reputation summary from settled player bets, public reason counts, final outcome accuracy, average entry confidence, Brier score, calibration score, payout, and final simulation-credit balance.
- Stored the reputation summary inside the canonical settlement object so room state, WebSocket settlement broadcasts, snapshots, event replay, replay/live verification, and public verification hashes all see the same projection.
- Added a compact public reputation projection for public verification artifacts with player counts, eligible counts, average calibration, and top public leaders without session IDs.
- Extended public recap and operator review generation with reputation/calibration evidence, metrics, integrity checks, recap lines, and single-room simulation guardrails.
- Updated TypeScript room, settlement, WebSocket, and public verification contracts.
- Updated README with the settlement, public verification, public recap, and WebSocket behavior.
- Added unit tests for the scoring math and redaction plus integration tests proving settlement response, state, replay, public projection, public verification, and recap/review text carry the summary.
- Full verification passed after the slice with 64 server tests, 67 Vitest tests, production build, bundle budget, smoke boot, and 34 isolated Playwright tests.

### Slice 13: Market Template Registry

Selected from mandatory priorities P5/A3/M2/D1/Protocol1/G11.

- Added `src/data/marketTemplates.json` as `market-template-registry/v1`.
- Registered playable `binary_over_under` plus draft-only `range_price_band`, `rent_yield_over_under`, `time_on_market_over_under`, and `renovation_budget_over_under` contracts.
- Added `server/marketTemplateRegistry.js` with cloned public registry output, playable validation, audit projections, and explicit rejection for registered-but-unplayable formats.
- Added public `GET /api/market-templates` so clients and future SDK/webhook consumers can discover supported and future market templates.
- Market Studio room creation now records an accepted market-template audit projection and rejects draft-only formats instead of silently falling back to binary.
- Added `src/lib/marketTemplates.ts` for typed registry access without forcing the full registry into the current app hot path.
- Updated Market Studio draft validation so the generated binary draft remains playable and draft-only formats produce honest local validation issues.
- Updated README with registry endpoint, playable/draft-only status, and the current non-binary engine boundary.
- Added server and frontend tests for registry cloning, playable binary validation, draft-only blocking, API output, and draft audit preservation.
- First full verification found a bundle budget regression; the fix kept the full registry out of the Join/Market Studio runtime bundle. Final verification passed with 67 server tests, 70 Vitest tests, production build, bundle budget, smoke boot, and 34 isolated Playwright tests.

### Slice 14: Generic Multi-Outcome LMSR Core

Selected from mandatory priorities P5/A3/M2/D1/Protocol1/G11.

- Added `src/lib/multiOutcomeMarketEngine.js` with `multi-outcome-lmsr-state/v1`.
- Implemented generic n-outcome LMSR log-sum-exp cost, softmax probabilities, rounded public projection, budget-to-shares execution, applied trade summaries, and winning-outcome settlement payout math.
- Kept zero-cost previews possible while rejecting zero-budget applied bets and zero-share applied trades.
- Updated `range_price_band` to use `lmsr_multi_outcome_v1` instead of `not_implemented`.
- Kept range markets `draft_only` at this slice boundary so room creation still blocked them until the follow-up range room API workflow landed.
- Updated server and frontend registry tests so the public registry proves the pricing core exists without claiming the workflow is playable.
- Updated README with the new pricing-core boundary and the continued non-binary room workflow limitation.
- Focused verification passed with 3 server registry tests, 8 Vitest registry/engine tests, typecheck, and diff whitespace checks.
- Full verification passed with client secret scan, typecheck, 67 server tests, 76 Vitest tests, production build, bundle budget, and local backend smoke boot.

### Slice 15: API-Playable Range Price-Band Rooms

Selected from mandatory priorities P5/A3/M2/D1/Protocol1/G11/Mega1.

- Added `server/roomMarketRuntime.js` to centralize room market format behavior across binary and range rooms.
- Marked `range_price_band` playable in the registry for server/API surfaces while keeping the rendered host/player UI and AI limitation explicit.
- Room creation now accepts range market drafts with optional `band_low` and `band_high`, stores `range-price-band-config/v1`, and emits `market_format` plus `market_config` in room-created events, state, snapshots, replay, and public projections.
- Range bets accept `below_band`, `inside_band`, and `above_band`, use the multi-outcome LMSR engine, preserve public bet reasons, and record selected-outcome entry probability.
- Range settlement resolves `actual_price` below/inside/above the configured band, pays winning shares, produces generic room reputation/calibration, and participates in replay/live verification.
- Public verification and replay integrity now hash market format/config as part of their public-safe projections.
- AI bot toggling is explicitly rejected for range rooms instead of silently running binary-only market logic.
- Updated market draft validation, README, registry tests, room event payload validation, public verification projection, and focused range workflow coverage.
- Focused verification passed with 29 server tests, 14 Vitest tests, and typecheck.
- Full verification passed with client secret scan, typecheck, 68 server tests, 76 Vitest tests, production build, bundle budget, and local backend smoke boot.

### Slice 16: Signed Range Verification Fixture

Selected from mandatory priorities D2/Protocol2/M2/G10/Trust1.

- Added `docs/fixtures/public-room-verification-range-price-band-v1.json`.
- The fixture pins a signed settled `range_price_band` public verification artifact with `inside_band` as the winning outcome.
- The fixture includes replay/live parity hashes, settlement evidence hash, reputation/calibration export fields, trust limitations, and HMAC signature metadata.
- Added server test coverage proving the range fixture verifies with the fixture secret and does not contain host tokens, player session IDs, or the signing secret.
- Updated README to document both binary and range public verification fixtures for SDK, webhook, and embed consumers.
- Verification passed with 5 public verification server tests and diff whitespace checks.
- Full verification passed with client secret scan, typecheck, 69 server tests, 76 Vitest tests, production build, bundle budget, and local backend smoke boot.

### Slice 17: Durable Identity-Backed User Reputation

Selected from mandatory priorities P7/U2/Trust1/Protocol1.

- Added `server/userReputationStore.js` with `fairvalue.userReputation.v1`.
- Signed-in players who join with a FairValue user identity now accumulate a private cross-room settlement projection with rooms played, total/correct bets, accuracy, reason count, wager/payout totals, Brier-style calibration, market-format counts, and recent room summaries.
- Added local JSON durability through `FAIRVALUE_USER_REPUTATION_PATH`, defaulting in the real server process to `.fairvalue/user-reputation.json` while tests can use in-memory or temp-file stores.
- Room snapshots and canonical event replay now preserve private `userIdsBySession` mappings so reputation can survive restart/replay without exposing `user_id` on public player state.
- Added token-protected `GET /api/me/reputation`; unauthenticated access is rejected, and recent room summaries omit player session IDs, host tokens, user tokens, and raw evidence.
- Settlement records user reputation only after the room settlement persistence path succeeds, and payout attribution is derived from the signed player's own winning shares instead of nickname matching.
- Updated README and `.env.example` with the private store and endpoint boundaries.
- Focused verification passed with 23 server tests, typecheck, and diff whitespace checks.
- Full verification passed with client secret scan, typecheck, 70 server tests, 76 Vitest tests, production build, bundle budget, and local backend smoke boot.

### Slice 18: Private Player Reputation Panel

Selected from mandatory priorities G4/G8/U2/UX1/Trust1.

- Added `useUserReputation` to fetch the signed-in player's private `/api/me/reputation` projection with the user token.
- Added `PlayerReputationPanel` with private rooms played, accuracy, calibration, reason count, wager/payout totals, recent settled room summaries, empty/error/loading states, and refresh control.
- Wired the panel into the mobile player room view so settled players see their own cross-room reputation after settlement, while active rooms show prior history only when it exists.
- Preserved the private/public boundary: the UI does not render the internal user ID, player session IDs, host tokens, user tokens, or raw evidence.
- Kept WebSocket settlement state aligned with the existing `reputation_summary` payload.
- Extended the rendered Chromium host/player flow to prove the settled player sees `My prediction record`, 100% accuracy for a correct room, the `1/1 correct` recent-room row, and simulation-credit trust copy.
- Added component coverage for populated and empty reputation states.
- The in-app Browser runtime remained unavailable (`iab`), so rendered evidence used the repo Playwright harness.
- The first full verifier run caught a bundle-budget regression. The panel was trimmed to avoid extra icon payload, and the total JS budget was explicitly raised from 760 kB to 765 kB; final total JS is 763.62 kB, with the largest chunk still under the existing 240 kB cap.
- Full verification passed with client secret scan, typecheck, 70 server tests, 78 Vitest tests, production build, bundle budget, and local backend smoke boot.

### Slice 19: Rendered Range Price-Band Room UI

Selected from mandatory priorities G4/G9/G11/P5/A3/M2/UX1/Protocol1.

- Added `roomMarketDisplay` helpers for market labels, range band labels, outcome labels, per-outcome probability lookup, leading-outcome selection, and range settlement outcome derivation.
- Extended typed room market payloads so host/player surfaces receive `market_format`, `market_config`, multi-outcome projections, and generic outcome maps without losing binary compatibility.
- Wired `useRoom` to preserve range market metadata and to keep optimistic market updates scoped to binary rooms where the two-outcome chart logic is still valid.
- Added rendered player range cards with range trust copy, outcome probabilities, range positions, and three outcome-specific bet controls for below/inside/above band.
- Added rendered host range surfaces: property summary copy, outcome probability bars, projector consensus/band metrics, range-aware settlement modal hints, activity feed labels, and range settlement result labels.
- Kept unsupported pieces honest: binary-only chart history and Cognee room intelligence are skipped for range rooms, and AI bot toggling remains blocked with a visible range limitation.
- Added Chromium e2e coverage that creates a range room through the API, opens host/player views, places an `Inside band` bet, settles inside the configured band, and verifies host/player settlement UI.
- Added regression coverage proving the existing binary host/player flow still passes after the generic range rendering work.
- The in-app Browser runtime remained unavailable (`iab`), so rendered evidence used the repo Playwright harness.
- The first full verifier run caught a bundle-budget regression from the new first-class range surface. The total JS budget was explicitly raised from 765 kB to 775 kB; final total JS is 770.76 kB, with the largest chunk still below the existing 240 kB cap.
- Full verification passed with client secret scan, typecheck, 70 server tests, 80 Vitest tests, production build, bundle budget, and local backend smoke boot.

### Slice 20: Private Prediction Profile Route

Selected from mandatory priorities U2/P7/UX1/Trust1.

- Added a lazy `/me` route for the signed-in player's private prediction profile.
- Linked the route from the market browser header so the reputation surface is reachable outside settled room screens.
- Reused the token-protected `useUserReputation` projection and `PlayerReputationPanel` while adding a broader recent-room history list with market format, winning outcome, score, calibration, wagered amount, and settlement date.
- Kept the privacy boundary visible and tested: the page is backed by the browser's signed user token but does not render the user ID, user token, host tokens, player session IDs, or raw evidence.
- Added focused component coverage for the profile route and a real rendered Chromium flow that mints an identity, seeds a settled room through the API, opens `/me`, and verifies private prediction history.
- Updated README routing, reputation, market-template, range UI, and e2e documentation so the docs match the current product surface.
- The in-app Browser runtime remained unavailable (`iab`), so rendered evidence used the repo Playwright harness.
- The first full verifier run caught a bundle-budget regression from the new lazy profile route. The total JS budget was explicitly raised from 775 kB to 785 kB; final total JS is 778.20 kB, with the largest chunk still below the existing 240 kB cap.
- Full verification passed with client secret scan, typecheck, 70 server tests, 81 Vitest tests, production build, bundle budget, and local backend smoke boot.

### Slice 21: Browser-Local Property Watchlist

Selected from mandatory priorities U2/UX1/Trust1.

- Added `usePropertyWatchlist` with versioned `fv_property_watchlist_v1` localStorage persistence, normalization, deduping, newest-first ordering, storage-failure tolerance, and non-browser fallback behavior.
- Property detail pages now expose an accessible watch/unwatch control with toast feedback.
- The private `/me` profile now renders watched properties by resolving local property IDs to the static property dataset, including address, location, asking price, added date, open link, loading/empty states, and a remove control.
- Kept the boundary honest: this is browser-local watchlist state, not server-synced account data, alerts, or notes.
- Extended profile route component coverage and Chromium e2e to add a property from `/market/:id`, prove it appears on `/me`, remove it, and prove it disappears while private prediction history remains redacted.
- Updated README with the private profile/watchlist mode and localStorage key.
- Full verification passed with client secret scan, typecheck, 70 server tests, 83 Vitest tests, production build, bundle budget, and local backend smoke boot.

### Slice 22: Static Property Data Manifest and Quality Contract

Selected from mandatory priorities G6/P4/Data1/UX1/Trust1.

- Added `scripts/property-data-manifest.js`, a deterministic manifest builder/checker for `public/data/properties.json`.
- Generated `public/data/property-data-manifest.json` as `fairvalue.propertyDataManifest.v1` with source file bytes/hash, 50-record count, provider summary, freshness window, tracked-field coverage, per-property critical missing fields, legal disclaimer digests, and static-provider limitations.
- Added `npm run data:manifest` and `npm run check:data`; wired `check:data` into `npm run verify` so property snapshot drift fails before typecheck/tests/build.
- Added server tests for provider summary, freshness, field coverage, source hash shape, critical gap detection, and legal disclaimer digest behavior.
- Added a typed frontend manifest loader and rendered a `Data quality contract` row on property pages with field coverage, critical-field status, latest source observation, provider summary, and source hash.
- Kept the boundary honest: this is a static provider snapshot contract, not live provider ingestion, global comps, permits, climate, insurance, or macro data.
- Updated README with manifest generation/check commands, architecture notes, integrations, verification behavior, and project structure.
- Extended Chromium profile e2e to prove the property-page data-quality contract renders before the watchlist/profile flow.
- Full verification passed with client secret scan, `check:data`, typecheck, 72 server tests, 83 Vitest tests, production build, bundle budget, and local backend smoke boot.

### Slice 23: Server-Backed Private Watchlist Notes and Thresholds

Selected from mandatory priorities U2/UX1/Trust1/Protocol1/D1.

- Added `server/userProfileStore.js` with `fairvalue.userProfile.v1` and `fairvalue.userWatchlist.v1` profile state.
- The profile store supports memory or JSON durability, defaults real local server runs to `.fairvalue/user-profile.json`, sanitizes private notes, normalizes saved price thresholds, dedupes watched property IDs, and keeps notification delivery out of scope.
- Added token-protected `GET /api/me/watchlist`, `PUT /api/me/watchlist/:propertyId`, `PATCH /api/me/watchlist/:propertyId`, and `DELETE /api/me/watchlist/:propertyId`.
- Extended `usePropertyWatchlist` so signed users merge browser-local watchlist rows with the server projection, upload local-only rows, expose sync state, and keep localStorage fallback behavior.
- `/me` now renders watchlist sync status, editable private notes, saved alert-below/alert-above thresholds, and clear `Saved thresholds only` trust copy.
- Market property watch buttons now sync through the signed user token when available.
- Extended Chromium profile coverage to add a property from the market page, verify signed sync, edit note and threshold fields, reload to prove server persistence, and remove the property.
- Updated README and `.env.example` with the private user profile path and watchlist API boundary.
- The first full verifier run caught a bundle-budget regression from the larger profile/watchlist surface. The total JS budget was explicitly raised from 785 kB to 790 kB; final total JS is 786.54 kB, with the largest chunk still below the existing 240 kB cap.
- Full verification passed with client secret scan, `check:data`, typecheck, 73 server tests, 84 Vitest tests, production build, bundle budget, and local backend smoke boot.

## Next Action

Continue toward the next highest incomplete mandatory item: implement actual alert evaluation/delivery over saved thresholds, introduce a real property query API over the manifest-backed snapshot, or add another playable non-binary market format.
